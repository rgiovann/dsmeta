export const BASE_URL = import.meta.env.VITE_BACKEND_URL ?? "https://rgiovann-dsmeta.herokuapp.com";

/* 
enviromnent var to request data from backend, if not in production default value is
http://localhost:8080
*/